import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    headerPrice: {
        width: '100%',
        height: 'auto',
        alignItems: 'center',
    },
    currentPrice: {
        color: '#c3ac18',
        fontSize: 32,
        fontWeight: 'bold',
        paddingTop: 20,
    },
    textPrice: {
        color: '#fff',
        fontSize: 16,
        fontWeight: 'bold',
    }
});

export default styles;